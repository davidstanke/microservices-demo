{
  "name": "microservices-demo-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root configuration for microservices-demo task orchestration",
  "scripts": {
    "setup": "npm --prefix scripts install",
    "generate-issues": "node scripts/generate-issues.js"
  }
}
